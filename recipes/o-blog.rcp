(:name o-blog
       :type github
       :description "Stand alone org-mode blog exporter. This installs my version with workaround for recent Twitter Bootstrap devs creating new org, breaking upstream o-blog dependencies."
       :pkgname "ajstein/o-blog"
       :branch "org-rename"
       :depends htmlize)
